# Comma-separated allowed browser origins for CORS (Vite / production frontend)
ALLOWED_ORIGIN=http://localhost:5173

# SQLite database (relative to project root). For production, switch to Postgres later.
DATABASE_URL="file:./prisma/dev.db"
